import React from "react";
import ReactDOM from "react-dom/client";
import HeaderHero from "./header-hero";
import "./CSS/header-hero.css";

ReactDOM.createRoot(document.getElementById("root-header")).render(
  <React.StrictMode>
    <HeaderHero />
  </React.StrictMode>
);
