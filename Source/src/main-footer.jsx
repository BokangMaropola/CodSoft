import React from "react";
import ReactDOM from "react-dom/client";
import FooterHero from "./footer-hero";
import "./CSS/footer-hero.css";

ReactDOM.createRoot(document.getElementById("root-footer")).render(
  <React.StrictMode>
    <FooterHero />
  </React.StrictMode>
);
